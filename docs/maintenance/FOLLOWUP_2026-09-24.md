# 2026-09-24 维护交付

基于当前工作区继续处理改动交付、跨平台验证、职责拆分和文档一致性。未更改 SDK/依赖版本、Shorebird 基线、数据库 schema 或发布版本；未自动提交、推送或发布原有改动。

## 工作区交付

本轮开始时 HEAD 为 `400040469ae1819034e4cf459bd5c8daf75b5b84`，有 100 个已跟踪修改、6 个删除和 130 个未跟踪文件，共 236 个路径。详见 [交付清单](WORKTREE_DELIVERY_2026-09-24.md)。该清单描述起始状态；后续新增的职责模块、回归和本文件属于本轮维护。

本地 `.dart_tool/maintenance-followup-20260924/baseline.json` 保留路径、状态与 SHA-256；只对本轮重构的少数源码保留本地副本。没有复制运行数据库、会话目录或凭据。按功能分组审阅共享文件的代码块，不能对整个工作区执行一次不区分来源的提交。

## 职责拆分

| 原入口 | 当前职责与新位置 |
| --- | --- |
| `core/network/community_service.dart` | 保留业务编排、身份、P1、缓存和公开 API；官网读取、formhash、私密小组表单提交与回执处理委托给 `community_website_client.dart` |
| `widgets/community_widgets.dart` | 保留兼容导出；`features/community/presentation/` 分别承载基础展示、话题/帖子卡片、动态卡片与共用回复/贴贴操作 |
| `screens/discover_page.dart` | 保留输入、防抖、筛选和导航；查询参数进入 `features/discover/domain/`，分页、请求代次与缓存恢复进入 `application/`，结果辅助组件进入 `presentation/` |

官网客户端按请求读取身份和失败回调，继续核验提交前后账号与会话；未知 POST 结果不重放。P1 所有写动词仍经过 `CommunityWriteClient`。发现页保留搜索输入即失效旧请求、缓存不能覆盖已完成网络结果、分页不能追加到新查询的规则。

没有用 `part` 把大型类机械分散到多个文件。已有页面导入与测试入口继续可用；应用层不依赖页面、Riverpod 或导航。后续如拆社区好友、日志和通知，应继续经过现有服务入口逐项迁移，避免把身份和缓存各复制一份。

## 验证与证据

编辑阶段运行发现页/控制器、社区服务与组件、P1 各写动词账号切换、私密小组发帖回执/网页会话变化、话题读取恢复和布局回归；再运行架构检查与完整验证。

```powershell
./tool/verify_all.ps1 -Mode Full -WindowsSmoke
```

完整记录由脚本写入 `.dart_tool/verification/<run>/results.json`；本轮最终结果另复制到 `.dart_tool/maintenance-followup-20260924/final-results.json`，保留原始日志路径和源码指纹。中途失败日志同样保留；不能用旧报告替代最终源码的验证。

Windows 冒烟使用独立测试入口、唯一临时安全存储键、内存 SQLite 和临时 WebView 配置，不启动带真实账号的应用。Quick、Full、原生插件测试和人工验收是不同证据层级。

原生测试会复用 Debug 产物目录。统一脚本在所有冒烟结束后再构建正常主应用，避免留下测试入口的可执行文件/APK；集成测试也使用 `--no-pub`，不在验证期间隐式重新解析依赖。

本轮设备探测仅发现 Windows 与浏览器，ADB 无设备，Flutter 无可用 Android 模拟器。以下项目保持未完成，不从自动化结果推断通过：

- Android 真机/模拟器插件冒烟、后台恢复及文件保存体验。
- 当前源码在 macOS 的 iOS 构建；CI 配置存在不代表已执行。
- 真实 OAuth、人机验证、会话失效、专用收件人私信与专用小组发帖。
- Android/Windows 系统文件对话框备份互导、提醒送达及整包更新安装。
- 公网部署、长期真实网络负载及实体打印。

上述真实写操作仍需要明确授权及专用测试对象；当前工作区也尚未达到干净源码发布条件。人工验收模板保持 pending，不填入模拟结果或编造时间。

## 文档一致性

README 的工具链和启动命令改为项目固定 SDK 入口；更新说明改为当前支持的暂停、跨进程续传、Range 回退与取消清理行为。历史架构审查保留原始发现，通过状态索引链接后续修复，避免把已经处理的问题重新当作现存缺陷。

2026-09-22 审查的 F1 账号重试、F2 开库恢复、F3 网站类型检查、F4 工具版本和 F5 当时的依赖告警已有后续实施记录；历史审计通过不代表未来依赖更新后仍无告警。2026-09-19 的 F1—F6 对应修复以当日修复记录和当前代码为准。
