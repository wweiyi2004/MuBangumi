# Changelog

## 未发布

- 收藏页增加多选与批量整理：支持全选当前结果、跨筛选保留选择、清空和退出；多选时可长按查看完整作品名。
- 批量改状态先展示数量、类型和目标，补齐本篇进度默认关闭；仅提交收藏状态，保留评分、吐槽、标签、隐私及书籍进度，并沿用离线同步队列。
- 批量加入新番表可指定年份、季度和星期，一次保存目标季度；已存在作品保留原安排、备注及提醒，新条目默认不开启提醒。
- 批量结果区分本机保存、未修改、保存失败和未开始；可仅重试保存失败项，返回时停止后续项目并保留未完成选择。
- 增强同步账号与版本保护：旧请求在刷新凭据后再次核对账号，旧失败记录不能重试覆盖后续修改；保留过时记录供核对，避免混用账号或丢失更早的章节完成操作。
- 番会荐卡片增加“不感兴趣”，即时隐藏作品并按账号保存在本机；访客记录独立保存，无需额外登录。
- 增加“已隐藏作品”管理与恢复入口，隐藏仅作用于该作品，不改变标签偏好；恢复后仍遵守收藏、评分和年份筛选。
- 推荐保留完整候选集合，在过滤隐藏和已收藏作品后再选取结果；隐藏后可补上其他候选，全部隐藏时提供恢复入口，并保留网络失败和部分结果提示。
- 反馈读取、保存和恢复失败提供重试，保存失败恢复原显示；账号切换和迟到请求不会混用旧记录。开始推荐后收起输入键盘，方便浏览结果。
- 新番表增加“今日／本周”列表，手机首次默认今日、Windows 默认周表；按账号记住视图选择，仍可切回原有周表和导出海报。
- 列表完整展示长名称、观看进度、提醒时间与 RSS 未读状态；待安排作品单独列出，历史季度明确显示所选季度的安排。
- 手机季度选择改为下拉菜单，桌面优先显示当前季度；收起次要工具按钮，并修复周表表头、待安排区域在大字体下的高度问题。
- 新番表跨午夜、恢复前台及本地时区变化后更新日期；通知续排重新读取设备时区，并按日历日期处理夏令时边界。
- 列表与周表复用安排菜单；切季度、保存中以及迟到的提醒权限返回不能修改错误的安排。提醒保存失败会在设置弹层内显示反馈。
- 首页支持按账号置顶作品，提供置顶管理、取消置顶及上下调整顺序；进行中预览始终按置顶顺序显示，完成或移除的作品退出首页列表。
- 首页、收藏页“看完下一集”及章节格子支持撤销最近一次修改；显示具体作品与章节，通常保留 10 秒，读屏导航模式下持续保留；失败可重试且不会覆盖后续修改。
- 撤销同时更新章节快照、进度数字和离线队列，支持原操作上传中或已上传后的恢复；连续点选多集按作品顺序保存，特别篇不再计入本篇进度。
- 章节缓存增加账号范围，统一详情页和格子的读取与更新，防止迟到结果覆盖新的章节状态；旧版无账号缓存不再用于恢复观看进度。
- 修复反复修改较早章节后队列顺序导致刷新进度回退，以及切换账号后新修改等待旧上传结束却未自动继续同步的问题。
- 改进窄屏大字体下的首页区块标题、章节图例和撤销提示，章节格子的高度随字体调整，深色未标记格子使用对应背景色。
- 私信写信与回复支持本机草稿自动保存：恢复收件人、标题和正文，回复按会话与话题分别保存；支持新建及找回其他写信草稿。
- 私信返回、切后台和切话题前补存，保存失败显示重试并阻止丢失输入；发送失败保留，发送成功后顺序清除，清理失败时锁定重复发送。
- 私信草稿按已核对的 Bangumi 账号隔离；同次应用内 OAuth 保存账号归属，旧网站会话核对自身账号后再恢复，普通验证码 Cookie 刷新无需重复核对。
- 回复话题按钮随字体大小调整高度，修复手机大字体下文字超出按钮背景的问题。
- 首页“继续追”增加查看全部入口，可直接浏览超出首页预览数量的进行中收藏。
- 发现页支持按账号保存最近搜索，点选历史会恢复搜索类型，支持清空；输入中的半截关键词不会自动记录。
- 收藏页记住作品类型、状态、进度、评分筛选和排序；快捷入口保持指定范围，迟到设置不会覆盖当前选择。
- 番会荐区分加载失败、部分结果和确实无匹配；支持重试，整次加载失败时保留上一次结果。
- 「我的」统计数字可直接打开对应状态、涵盖全部作品类型的收藏列表，并支持返回原页面。
- 首页和收藏页显示待同步、正在同步及同步失败状态，提供立即同步和同步问题入口；同步完成后自动收起。
- 修复私信改填收件人后仍使用旧收件人参数，以及迟到校验覆盖新收件人的问题。
- 私信表单绑定网站认证会话，账号变化后拒绝旧响应和旧表单发送，并清除旧会话内容；普通验证码 Cookie 刷新不影响草稿。
- 修复切换私信话题加载失败后仍可使用旧话题表单回复的问题。
- 串行执行 Windows 网站 Cookie 清理和注入，避免退出后的后台清理删除新登录。
- 修复写信页确认提示在窄屏大字体下溢出，并检查异步回复完成时的页面生命周期。

## 2.1.1 - 2026-09-07

- 长标题增加换行空间，截断名称可点按打开完整、可选择的文本；条目详情取消标题行数限制。
- 重做「我的」收藏统计面板：总收藏使用 52 号数字，进行中与已完成使用 36 号数字，窄屏和大字体自动调整布局。
- 手机与 Windows 应用内 OAuth 授权共用网页 Cookie，账号验证成功后同步保存；私信与小组网页登录自动保存，减少重复输入和手动同步。
- 手机授权回调直接交回应用，保留 state 与回调地址校验；退出或取消后的旧回调与旧会话捕获不再被接受。个人 Token、系统浏览器授权和过期会话仍可通过网站登录补充。
- 社区发帖与回复草稿按账号和回复对象持久化，重启后可恢复，发送成功后清除。
- 私信收发件箱支持独立分页、去重和失败重试，刷新保留内容，发送后更新列表。
- 收藏手账支持将完整 JSON 另存到自选位置或通过系统分享。

### 发布

- 版本号 `2.1.1+11`，Shorebird Flutter `3.44.7`。
- 提供 Android 正式签名 APK、Windows x64 ZIP 与 SHA-256 校验文件。
- 399 项自动化测试通过，静态检查无问题。

## 2.1.0 - 2026-09-05

### 新功能

- Android / Windows 发布包使用 Shorebird 引擎，支持 OTA 代码更新；启动和回到前台时在后台检查，登录界面也可接收修复。
- 收藏统计升级为个人收藏手账，加入昵称、偏好标签、年度月度图表与可筛选的收藏片段；评分趋势可只看我的收藏并展示个人评分。

- 收藏与章节修改改为离线优先：先写入按账号隔离的 SQLite 队列，断网时立即更新本地状态，联网、回到前台或手动同步后按顺序补传。
- 章节状态增加 30 天本地快照，已打开过的章节表在断网时仍可查看和修改。
- 新番表支持为每部番独立开启系统更新提醒并设置时间；改期、移除和删除季度时会自动重排或清理通知。

### 改进

- OTA 启动检查与手动检查合并，补丁公告严格匹配基线及补丁编号；支持稍后重启，保留 GitHub 整包升级入口。
- 社区列表缓存和网络并行加载，刷新保留内容，分页失败可重试；回复后保留阅读位置，编辑框支持页面内临时草稿。
- 图片按显示尺寸解码，评分历史和社区请求使用有界缓存；RSS 限制并发，后台暂停轮询，收藏分页加载减少等待。

- 首页优先展示正在追的作品，统计缩为摘要；发现页提前搜索入口，搜索时隐藏评分趋势推荐。手机底部导航常驻文字，并统一主要页面的标题与顶部间距。
- 减少首页和收藏页的重复加载提示，已有内容同步时保持页面可读；小屏将二维码和扫一扫收进更多菜单。
- 精简登录、个人页、私信、百科阅读和更新等界面文案，移除实现说明与重复提示；保留操作后果、数据来源和第三方线路的隐私说明。
- 一键登录与备用 Token 登录增加授权、账号验证、首页准备的阶段进度；首页进入采用轻量淡入和位移动画，并尊重系统减少动画设置。
- 首次登录优先加载首页动画收藏，离线上传与其他类型收藏不再阻塞首页；准备页可直接跳过，最多等待 8 秒，慢网与加载失败均提供明确反馈。
- 登录状态独立于收藏同步，明确区分等待授权、验证账号和退出清理；网络异常时保留凭据，并提供重新连接已保存登录的入口。
- OAuth 凭据与应用配置在账号验证成功后整组保存；个人 Token 登录清除旧刷新凭据，兼容读取旧版存储。
- 网站登录标明为私信等网页功能的补充会话，拒绝保存仅含挑战 Cookie 或已过期登录 Cookie 的结果。
- 同一收藏或单集的连续离线修改只保留最后值；上传确认使用版本校验，避免较旧的在途请求删除较新的本地修改。
- 同步失败采用 20 秒到 5 分钟的退避重试；非临时服务端拒绝会保留记录，用户可在「我的 → 立即同步」手动重试。
- 永久同步失败可在「我的 → 同步问题」查看具体条目、操作和错误原因，并支持单条/全部重试或停止上传；停止上传时会清理受影响的本地快照。
- 私密小组发帖在 P1 成员校验失败时回退到官网表单，与回复走同一条网站通道。
- 官网回退发帖/回帖时从页面或首页登出链接读取 formhash；会话无效时提示重新同步网站登录，而不再报「无法获取网页版表单参数」。

### 修复

- 修复取消授权、切换备用登录、退出和刷新凭据交错时旧请求覆盖新会话的问题；取消时同时释放回调端口并中止 Token 交换。
- 串行保存和清理登录凭据、网站会话，避免退出后旧写入或旧读取恢复登录；缓存异常不再阻止已验证账号进入应用。
- 系统提醒持久化通知 ID，修复 Windows 关闭、改期或删除新番表后旧通知仍保留的问题，并兼容旧通知迁移。
- 串行重排系统提醒并忽略过期的新番表读取结果，避免已关闭的提醒被较旧请求重新开启。
- 停止上传自动补齐章节的收藏任务时，同时清理章节快照，避免离线界面继续显示错误的看过状态。

### 发布

- 版本号 `2.1.0+10`，Shorebird Flutter `3.44.7`。
- 提供 Android APK、Windows x64 ZIP 和 SHA-256 校验文件。

## 2.0.0 - 2026-08-16

### 新功能

- 自动检测 GitHub Release 整包更新，登录后弹 Markdown 公告；可前往下载、稍后或跳过此版本。
- 「我的」入口改为「检查更新」，同时覆盖 Shorebird 热更新和安装包升级。

### 改进

- 跳过只作用于当前 tag，下一个正式版仍会提示；手动检查仍会展示已跳过的版本。

### 发布

- 版本号 `2.0.0+9`。

## 1.7.0 - 2026-08-16

### 新功能

- 用户主页可查看与对方的共同好友。
- 网络线路选择支持测速，并提示反代瞬时失败时可重试。
- 发现页浏览年份可手动输入；季度浏览允许下一年，方便提前看新季度。
- 仓库新增独立的 Kotlin / Compose 原生 Android 原型（`native-android/`），非正式发布包。

### 改进

- 反代 GET 遇到超时或 502/503/504 时自动重试一次。
- 条目、RSS、更新说明和社区外链只打开 http(s)，避免 intent / javascript 等自定义协议。
- 社区内嵌页只嵌入 Bangumi 域名；`javascript://bgm.tv` 这类伪装地址会被拦截。
- Android 关闭备份，避免把设备绑定凭据拷到云端。

### 发布

- 版本号 `1.7.0+8`。

## 1.6.0 - 2026-08-15

### 新功能

- 电波提醒改为应用内打开：小组 / 条目 / 角色 / 章节 / 人物 / 日志类通知进入原生话题页。
- 回复吐槽打开自己的时光机并定位到对应动态；在吐槽中被提及时打开发送者的时光机。

### 改进

- 时光机深链用 `until` 锚定目标动态，找不到时提示，不再静默停在最新一页。
- 修复 Windows 上 Cloudflare Turnstile 验证码加载较慢时丢 token 的问题。
- 私密小组回复在 P1 失败时回退到官网表单。

### 发布

- 版本号 `1.6.0+7`。

## 1.5.0 - 2026-08-13

### 新功能

- 小组与条目讨论原生显示“贴贴”反应，并支持添加、切换和取消官网表情。
- 电波提醒展示发起人、话题标题与回复正文；好友申请可在客户端定向接受。
- 好友二维码支持保存、分享和更清晰的扫码引导；新增“扫码 / 我的二维码 / 今日放送”系统快捷入口。
- 发现页加入 Bangumi 官方分类标签，社区正文支持原生 Bangumi 表情渲染。

### 改进

- 修复小组回复时 Cloudflare Turnstile 验证反复失败的问题，按官方 P1 契约发送回复。
- OAuth 登录回调后自动关闭内置浏览器，社区时间线补全缺失头像。
- 修复 Android 静态与动态快捷方式重复注册带来的失效风险。

### 发布

- 版本号 `1.5.0+6`。

## 1.4.0 - 2026-08-13

### 新功能

- 本客户端二维码加好友：在「我的」出示名片码，在「我的好友」扫描；手机走摄像头，Windows 选图识别。
- 条目详情接入萌娘百科面板。
- 冷启动用上次登录用户 + 收藏快照直接进入主界面，不再先卡在启动页等 `/me`。

### 改进

- 社区时光机「我的」补全缺 `user` 字段的条目，缓存读取与直播请求使用同一套身份回退。
- 桌面追番页「电波提醒 / 同步收藏」固定在窗口右上角。
- 列表封面按场景分级：海报墙用大图，小缩略图用较小尺寸。
- 登录竞态、Cookie 复活、私信成功判定、网络线路切换卡死等一批会话与社区缺陷修复。
- 增加本机 OAuth 构建脚本 `tool/build_release.ps1`。

### 发布

- 版本号 `1.4.0+5`。后续仅改 Dart 时可走 Shorebird patch；新增扫码原生插件后需重新 `shorebird release`。

## 1.3.0 - 2026-08-11

### 新功能

- 站内短信改为原生收件箱 / 会话 / 发送（网站 Cookie + HTML），失败可回退网页版。
- 「我的」支持同步网站登录：安全存储 Cookie，供私信等网页能力使用；退出 OAuth 时一并清除。
- 接入 Shorebird 热更新：启动与手动检查、下载 patch、Markdown 更新说明与重启生效。
- 收藏与发现页本地快照 + stale-while-revalidate，冷启动优先展示缓存。

### 改进

- OAuth 授权对话框与 Windows 启动相位修复，登录态更稳定。
- 社区相关页面配合本地缓存与会话状态调整。
- README 补充 Shorebird release / patch 发布说明。

### 发布

- 首个 Shorebird 基线 release（Windows + Android），后续 Dart 改动可走 patch 热更新。

## 1.2.0 - 2026-08-11

### 新功能

- 新增公司事实档案：按年份和职位浏览公司的关联作品，显示参与集数、年份资料补全进度与事实来源口径。
- 人物搜索结果新增公司 / 团体类型标识，公司和普通人物进入不同的信息布局。
- 条目详情的角色、制作人员和关联作品改为可浏览卡片；制作人员按职位分组并支持展开全部职位。

### 设计改进

- 参考 Pixiv-Shaft 的内容优先范式重做视觉层级，首页与发现页采用海报化卡片、紧凑导航和响应式布局。
- 首页增加新番表、每日放送、番会荐和找新番快捷入口；桌面侧栏重新组织主要页面与工具入口。
- 公司长简介默认折叠，同一作品的多条职位关系自动归并，避免重复卡片和超长事实列表。
- 新番表页面在独立打开时显示明确的返回按钮。

### 数据与性能

- 扩展人物 / 公司关联数据，保留公司类型、职位和参与集数。
- 公司作品年份采用限并发分批补全与会话缓存，失败可重试，避免一次请求数百个条目详情。
- 对年份解析、公司职位归并、工作人员职位分组和海报卡片布局增加自动化测试。

## 1.1.0 - 2026-08-10

### 新功能

- 新增用户收藏口味对比，综合收藏重合度、共同评分相关性、评分差和样本置信度。
- 新增收藏统计与年度回顾，支持类型、状态、评分和标签分布，以及完整 JSON 导出。
- 新增仅保存在本机的用户备注与内容屏蔽，屏蔽内容可以临时展开。
- 社区编辑器新增 BBCode 工具栏、长内容折叠和旧话题回复提醒。

### 改进

- 优化社区发帖与回复的 P1 / Turnstile 登录流程及错误处理。
- 改进好友、提醒、用户主页和社区时间线体验。
- 修复退出登录期间提醒请求可能回写旧状态的问题。
- 发现结果和长话题改为懒构建，超长章节分批显示。
- 壁纸按屏幕尺寸解码并减少重复模糊，收藏搜索增加防抖，JSON 导出移至后台 isolate。
- 首页标签页按需初始化，降低首次进入时的工作量。

### 发布

- Android 正式构建改用独立上传密钥；缺少签名配置时主动阻止误发调试签名包。
- Android `compileSdk` 升级为 36，以兼容当前 Flutter 插件。

## 1.0.0 - 2026-08-06

- 首个公开版本，支持 Android 与 Windows。
