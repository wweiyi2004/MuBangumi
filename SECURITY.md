# 安全问题与诊断数据

不要在公开 issue、日志或截图中附上 Token、Cookie、OAuth Secret、私信正文、联系人或完整数据库。

如仓库启用了 GitHub 私密漏洞报告，请通过该入口联系维护者。否则先提交不包含利用细节或个人数据的问题，请求维护者提供私下沟通方式；不要猜测联系邮箱。

普通登录问题可在“设置 → Bangumi 账号 → 登录诊断”核对并导出记录。记录仅包含本次运行的固定事件、状态、请求编号、耗时、应用/补丁与浏览器主要版本；它保存在内存中，由用户主动导出。

维护者应按周查看 Dependabot，按发布节奏运行网站依赖审计，并对 Python 锁文件更新重跑对应测试。打包路径检查不替代秘密扫描；也不表示所有部署环境都不存在漏洞。

当前代码与素材的统一开源许可证仍需项目所有者明确。未使用的 D1 模板已归档于 docs/archive，不属于网站活动依赖；第三方包和数据各自的授权说明仍应保留。
