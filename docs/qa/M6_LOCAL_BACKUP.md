# M6 本地备份与导入验收记录

日期：2026-09-08。状态：功能及应用内入口已实现，自动化验证完成；原生设备互导和安装产物待验收，未发布。数据层提交 `7aae41f`，文件适配提交 `9e3d1f1`，界面及联动提交 `f1346dd`。正在准备 `2.2.0+12` 验收包，已发布基线仍为 `2.1.1+11`；本阶段未上传 Release 或 Shorebird 补丁。

## 当前实现

- 单个 UTF-8 JSON 文件，格式标识 `mubangumi-backup`、版本 `1`，包含账号 UID／用户名、UTC 导出时间、类别及 SHA-256 校验值。严格验证字段、类型、记录标识、引用、排序、大小及嵌套深度。文件不超过 16 MiB，记录总数不超过 50,000，搜索历史最多 12 条。
- 校验值基于递归按键排序的 JSON，数组顺序保留；校验值用于发现损坏，不是签名。界面说明备份是明文文件，导出和导入默认关闭两个草稿类别。
- 数据白名单包含新番表、RSS 源与过滤规则、备注／屏蔽、置顶、浏览偏好／搜索历史、推荐反馈、社区草稿和私信草稿。不会附加数据库文件、凭据、Cookie、可重放队列、RSS 条目缓存、提醒 ID 台账或草稿删除记录。
- 新番表、RSS、备注／屏蔽沿用现有设备共享范围；置顶、浏览偏好、反馈和草稿按当前账号读取。不同 UID 的文件被拒绝；同 UID 改名时，用户名键重新绑定当前用户名。
- 旧版新番表缺失的三个提醒字段在导出读取时补为关闭、20:00；其余损坏或未知字段仍报错，不静默删除安排。

## 冲突与恢复行为

合并保留本地冲突项，加入不存在的记录；同季度内保留原作品的所有字段，只追加新作品。置顶、搜索、RSS 规则和私信草稿保留本地顺序后追加导入项。搜索合并超出 12 条时，计划明确计入未加入项。

覆盖仅替换选择的类别，包括清除这些类别中备份没有的记录。未选择类别和其他账号的数据保持原样。预览保存所选数据的摘要，执行时在事务中重新读取、核对并重新生成计划；预览后有变化就拒绝继续。

SQLite 协调数据库使用持久文件，将最多六个个人数据库作为固定别名附加。所有参与者必须使用 DELETE 回滚日志，并启用 FULL 同步；不允许内存主库、重复路径或 WAL。导入日志和个人数据在同一个事务中提交。日志只记录账号、文件校验值、模式、类别和时间，不记录正文。

这遵循 SQLite 对多文件原子事务的条件，见 [ATTACH 官方说明](https://www.sqlite.org/lang_attach.html) 与 [原子提交机制](https://www.sqlite.org/atomiccommit.html)。当前故障实验覆盖事务提交前强制退出，不等同于对真实设备断电或任意存储硬件故障的验证。

RSS 本地 ID 在导入时重新分配，源与规则引用同步转换；源和规则未改变时保留并重映射原缓存，规则或匹配顺序改变时清除相应缓存及抓取验证信息。备份本身不包含 RSS 条目。

社区草稿库升级至 v2，原有文本保留。社区及私信导入更新草稿版本，删除保留无正文的版本记录，旧编辑器迟到的保存／清理会被拒绝。社区草稿读取失败时禁止编辑和发送，重试成功后恢复；保存冲突保留当前输入，并提供不保存关闭。

## 应用内流程及任务衔接

入口为“我的 → 本地备份与导入”。导出先选择类别、说明待同步数量，再显示备份内容数量，支持保存文件和系统分享。读取按实际字节流限长，拒绝空文件、截断、超限及损坏内容；文件 JSON 编解码使用工作 isolate。取消保存或保存失败时保留准备好的备份以便重试。

导入显示来源账号、时间和各类别数量，先选合并／覆盖，再核对新增、保留、替换、移除和未加入的数量；明细列表可查看完整名称。预览按季度计新番表变更并另列作品总数。页面明确说明设备共享范围。各步骤切换重置滚动位置，窄屏先显示预览说明；失败时回到错误信息，执行失败需重新预览。换文件校验失败不会继续使用旧选择，账号变化隐藏旧内容和明细，重复点击不会重复执行。

导入前暂停新番表与 RSS 控制器，等待已接受的保存和已提交的提醒同步。迟到的 RSS 成功、304 或失败结果均不能写回旧源信息。按账号记录尚未进入存储层的偏好写入，关闭页面后仍等待其保存完成，再核对预览摘要。

完成后重读安排、重排提醒并使所选类别的偏好视图失效；收藏页重读筛选而不重建登录或导航，空季度不会在刷新时自动写回。RSS 本次运行暂缓自动抓取，手动检查后恢复。读取失败时新番表阻止旧内容编辑并提供重试，RSS 也必须先成功读取配置才能检查更新。提交后的读取／提醒失败作为提示显示，不把已经提交的导入报告为写入失败。

## 验证证据

| 检查 | 实际结果 |
| --- | --- |
| `flutter analyze --no-pub lib test tool/qa/backup_crash_probe.dart` | 无问题 |
| `flutter test --no-pub --reporter expanded` | 747 项通过 |
| 真实字体界面矩阵 | 34 项界面测试通过；导出选择、备份就绪、导入选择、覆盖预览、导入结果及草稿冲突，共 6 个状态 × 320／390／1200 宽 × 1／1.8 倍字体 × 深／浅色，72 张截图 |
| 真实 SQLite 往返 | 八类数据导入空白库，关闭重开后关键字段与摘要一致；私信正文、RSS 引用、置顶及搜索顺序保留 |
| 隔离 | 其他账号及未选类别不变；错误账号在打开目标文件前拒绝；同 UID 改名重新绑定用户名键 |
| 重复与并发变化 | 相同文件重复导入不新增日志或草稿版本；预览后变化、执行中账号变化拒绝或回滚 |
| 逐步写入失败 | 分别在八个类别写入后注入异常，全部个人数据库及导入日志回滚；重开后能成功重试 |
| 实际进程退出 | 独立 Flutter 测试进程写入约 8 MB 合成数据，七个回滚日志存在，备注与社区库原始文件均已增长超过 1 MB 后直接 `exit(77)`；父进程重开后所有个人数据与导入前一致，日志为空，再次导入成功 |
| 兼容与草稿 | 社区草稿 v1 迁移、删除版本重启保护、旧新番表字段兼容、私信与社区迟到保存／清理拒绝通过 |
| RSS 缓存 | 仅修改源名称保留缓存并重映射 ID；改变匹配规则清除对应条目和 ETag |
| 后台任务及视图 | 真实 SQLite 配合控制器验证迟到的 RSS 成功／304／失败、事务失败恢复、提醒权限迟到、关闭页面后的偏好排队写入；读取失败阻止使用旧内容；收藏筛选刷新保持原页面和登录对象 |
| 界面保护 | 导出／文件选择／预览／明细／执行中账号变化隐藏旧数据；草稿默认关闭，损坏文件更换、取消保存、保存失败、过期预览重试及重复执行保护通过 |

主要测试：`backup_archive_test.dart`、`backup_sqlite_test.dart`、`backup_crash_recovery_test.dart`、`backup_files_test.dart`、`backup_runtime_test.dart`、`backup_page_test.dart`、`community_draft_import_experience_test.dart`，以及原有草稿、新番表、RSS 和浏览回归。进程退出探针为 `tool/qa/backup_crash_probe.dart`，只接受父测试创建的临时目录；子进程有独立构建目录，避免 Windows 已加载 SQLite DLL 的文件锁冲突。

最新日志位于忽略目录 `.dart_tool/m6-ui-final-analyze.log`、`.dart_tool/m6-ui-full-tests.log`、`.dart_tool/m6-final-visual-tests.log`、`.dart_tool/m6-runtime-final-tests.log`。截图为 `.dart_tool/m6_backup_*.png` 与 `.dart_tool/m6_draft_conflict_*.png`，已检查代表性的手机／桌面、深浅色和大字体画面；它们是 Flutter 渲染测试截图，不是真机截图。全部数据为临时合成数据，没有导入或修改真实用户数据，也没有发送真实消息。

## 继续实施与待验收

- Android／Windows 原生文件对话框、系统分享、进程重启、备份互导和安装产物核对。
- 真机性能基线、实际官网及前述各阶段设备验收。当前未检测到 Android 设备，已向用户询问连接手机的安排，未将此项记为通过。

功能与自动化通过不代表已经发布，也不代表 M6 整体或长期目标已经完成。
